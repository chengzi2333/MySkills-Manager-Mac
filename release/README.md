# Release Artifacts

`Skillar.exe` is the visible launcher copy for local use.

Generate or refresh it with:

```bash
npm run build:desktop
```

Or if you already built Tauri:

```bash
npm run expose:exe
```
